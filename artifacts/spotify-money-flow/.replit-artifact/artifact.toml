kind = "web"
previewPath = "/"
title = "Spotify Money Flow"
version = "1.0.0"
id = "artifacts/spotify-money-flow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23239

[services.development]
run = "pnpm --filter @workspace/spotify-money-flow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/spotify-money-flow", "run", "build" ]
publicDir = "artifacts/spotify-money-flow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23239"
BASE_PATH = "/"
